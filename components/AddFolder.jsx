import React, { useState } from "react";
import data from "./data.json";

// Button to add folder
// Folder is connected to a User
// Folder is placed outside the /pages/ scope
//
// const AddFolder = ({ addFolder }) => {
//     const [userFolders, addFolder] = useState(data);
//
//
//     return <button>Add New Folder</button>;
// };
//
// export default AddFolder;